name: Test kubeconform lint with multiple manifests
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Install kubeconform
        run: |
          ARCH=$(uname -m)
          if [ "$ARCH" = "x86_64" ]; then ARCH="amd64"; fi
          curl -sSL "https://github.com/yannh/kubeconform/releases/latest/download/kubeconform-linux-${ARCH}.tar.gz" \
            -o /tmp/kubeconform.tar.gz
          tar -xzf /tmp/kubeconform.tar.gz -C /tmp
          chmod +x /tmp/kubeconform
          sudo mv /tmp/kubeconform /usr/local/bin/kubeconform
          kubeconform -v

      - name: Run kubeconform lint on multiple valid manifests
        id: action
        uses: .
        with:
          manifests: |
            tests/fixtures/valid-deployment.yaml
            tests/fixtures/valid-service.yaml
          lintType: kubeconform
          kubeconformOpts: '-summary'

      - name: Assert action succeeded
        run: |
          echo "PASS: kubeconform lint succeeded on multiple valid manifests"
